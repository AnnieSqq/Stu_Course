#include "act_cor.h"
#include "act_stu.h"
#include <iostream>
using namespace std;
int main()
{
    do
    {
        string key = "";
        puts("请输入要进行的操作代号：");
        puts("as-添加学生");
        puts("ds-删除学生");
        puts("ms-修改学生");
        puts("ss-查找学生");
        puts("ls-列出学生");
        puts("ac-添加课程");
        puts("dc-删除课程");
        puts("mc-修改课程");
        puts("sc-查找课程");
        puts("lc-列出课程");
        puts("q-退出");
        cin >> key;
        if (key == "as")
        {
            puts("----------------------------------- --     添加学生   ----------------------------------");
            if (addStu() > 0)
            {
                puts("-------------------------------------添加学生成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------添加学生失败----------------------------------");
            }
        }
        else if (key == "ds")
        {
            puts("----------------------------------- --     删除学生   ----------------------------------");
            if (deleteStu() > 0)
            {
                puts("-------------------------------------删除学生成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------删除学生失败----------------------------------");
            }
        }
        else if (key == "ms")
        {
            puts("----------------------------------- --     修改学生   ----------------------------------");
            if (modifyStu() > 0)
            {
                puts("-------------------------------------添加修改成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------添加修改失败----------------------------------");
            }
        }
        else if (key == "ss")
        {
            puts("----------------------------------- --     查找学生   ----------------------------------");
            if (lookStu() > 0)
            {
                puts("-------------------------------------查找学生成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------查找学生失败----------------------------------");
            }
        }
        else if (key == "ls")
        {
            puts("----------------------------------- --     列出学生   ----------------------------------");
            int total = 0;
            if ((total = listStus()) > 0)
            {
                printf("-------------------------------------个数：%d---------------------------------",total);
            }
            else
            {
                puts("-------------------------------------还没有学生----------------------------------");
            }
        }
        else if (key == "ac")
        {
            puts("----------------------------------- --     添加课程   ----------------------------------");
            if (addCourse() > 0)
            {
                puts("-------------------------------------添加课程成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------添加课程失败----------------------------------");
            }
        }
        else if (key == "dc")
        {
            puts("----------------------------------- --     删除课程   ----------------------------------");
            if (deleteCourse() > 0)
            {
                puts("-------------------------------------删除课程成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------删除课程失败----------------------------------");
            }
        }
        else if (key == "mc")
        {
            puts("----------------------------------- --     修改课程   ----------------------------------");
            if (modifyCourse() > 0)
            {
                puts("-------------------------------------修改课程成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------修改课程失败----------------------------------");
            }
        }
        else if (key == "sc")
        {
            puts("----------------------------------- --     查找课程   ----------------------------------");
            if (lookCourse() > 0)
            {
                puts("-------------------------------------查找课程成功----------------------------------");
            }
            else
            {
                puts("-------------------------------------查找课程失败----------------------------------");
            }
        }
        else if (key == "lc")
        {
            int total = 0;
            puts("----------------------------------- --     列出课程   ----------------------------------");
            if ((total = listCourses()) > 0)
            {
                printf("-------------------------------------个数：%d----------------------------------", total);
            }
            else
            {
                puts("-------------------------------------添加学生失败----------------------------------");
            }
        }
        else if (key == "q")
        {
            puts("----------------------------------- --     退出程序   ----------------------------------");

            puts("-------------------------------------欢迎下次光临----------------------------------");

            puts("------------------------------------   -再见----------------------------------");
            break;
        }
        else
        {
            puts("------------------------------------您的输入俺不认识----------------------------------");
        }
    } while (1);
    return 0;
}
